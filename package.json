{
  "name": "aga",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy" : "echo '' > dist/.nojekyll && gh-pages -d dist -t true"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
